interface:
  display_name: "Experimental Assistant (Compatibility)"
  short_description: "Legacy $ea-v0-2 entry for the v0.9.8 $ea skill"
  default_prompt: "Use $ea-v0-2 only as a compatibility entry, then follow the installed $ea skill without rewriting historical project identifiers."
